import styled from 'styled-components';

const Sponsors = styled.div`
  display: flex;
  align-items: center;

  > img {
    margin-right: 2rem;

    &:last-child {
      margin-right: 0;
    }
  }
`;

export default Sponsors;
